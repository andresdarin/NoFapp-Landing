export function SectionHeading({ eyebrow, title, subcopy, align = "left" }: { eyebrow: string; title: string; subcopy?: string; align?: "left" | "center" }) { return <div className={align === "center" ? "mx-auto max-w-2xl text-center" : "max-w-2xl"}><p className="mb-4 text-xs font-bold uppercase tracking-[0.22em] text-primary">{eyebrow}</p><h2 className="font-display text-4xl uppercase leading-[.95] tracking-tight text-foreground sm:text-6xl">{title}</h2>{subcopy && <p className="mt-5 max-w-lg text-base leading-relaxed text-muted">{subcopy}</p>}</div>; }
