import { cn } from "@/lib/utils";
export function Badge({ children, tone = "active", className }: { children: React.ReactNode; tone?: "active" | "fallen" | "success"; className?: string }) { return <span className={cn("inline-flex items-center gap-1.5 rounded-full border px-2.5 py-1 text-[10px] font-bold uppercase tracking-[0.16em]", tone === "active" && "border-primary/30 bg-primary/10 text-primary", tone === "fallen" && "border-muted/20 bg-muted/10 text-muted", tone === "success" && "border-success/30 bg-success/10 text-success", className)}><i className={cn("size-1.5 rounded-full bg-primary", tone === "fallen" && "bg-muted", tone === "success" && "bg-success")} />{children}</span>; }
