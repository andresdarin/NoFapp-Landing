"use client";
import { useState } from "react"; import { Check, RotateCcw } from "lucide-react"; import { Button } from "@/components/ui/button"; import { Surface } from "@/components/ui/surface"; import { copy } from "@/content/copy";
export function CheckinDemo() { const [checked, setChecked] = useState(false); return <section className="px-5 py-20 sm:py-28 lg:px-8 lg:py-36"><Surface className="mx-auto max-w-4xl border-primary/40 bg-surface-raised p-6 shadow-lg sm:p-12"><div className="grid gap-10 sm:grid-cols-[1fr_auto] sm:items-center sm:gap-12"><div><p className="mb-4 text-xs font-bold uppercase tracking-[.22em] text-primary">{copy.checkin.eyebrow}</p><h2 className="font-display text-5xl uppercase leading-[.9] sm:text-7xl">{copy.checkin.title}</h2><p className="mt-5 text-sm text-muted">A tiny daily ritual with a surprisingly large payoff.</p></div><div className="text-left sm:text-right" aria-live="polite" role="status"><span className="text-xs font-bold uppercase tracking-widest text-muted">Current streak</span><strong className="mt-2 block font-display text-8xl leading-none text-primary">{checked ? "07" : "06"}</strong><p className="mt-2 font-display text-xl">{checked ? copy.checkin.unlocked : "days"}</p></div></div><div className="mt-8 flex flex-wrap items-center gap-4 border-t border-border pt-6 sm:mt-10 sm:pt-7"><Button onClick={() => setChecked(true)} disabled={checked}>{checked ? <><Check className="mr-2 size-4" />Checked in</> : copy.checkin.cta}</Button><button type="button" aria-label="Reset demo" onClick={() => setChecked(false)} disabled={!checked} className="inline-flex min-h-11 items-center gap-2 text-xs text-muted hover:text-foreground disabled:pointer-events-none disabled:opacity-50"><RotateCcw className="size-3" /> {copy.checkin.reset}</button></div></Surface></section>; }
