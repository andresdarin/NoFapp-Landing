import { SectionHeading } from "@/components/ui/section-heading"; import { ranks } from "@/data/ranks";
export function RanksShowcase() { const shown = ranks.filter((rank) => [0, 1, 3, 7, 12, 30, 90].includes(rank.day)); return <section id="ranks" className="mx-auto max-w-6xl scroll-mt-24 px-5 py-20 sm:py-28 lg:px-8 lg:py-36"><SectionHeading eyebrow="The long game" title="From nobody to untouchable" subcopy="Every day unlocks a new version of you. The mysterious ones stay mysterious until you earn them."/><ol className="relative mt-12 grid gap-7 sm:mt-16 sm:grid-cols-2 lg:grid-cols-7 lg:items-start lg:gap-3">{shown.map((rank, index) => <li key={rank.day} className={`relative rounded-xl border border-border bg-surface p-4 ${index % 2 ? "lg:mt-14" : index > 3 ? "lg:mt-28" : ""}`}><div className="mb-3 flex items-center gap-3"><span className="flex size-10 shrink-0 items-center justify-center rounded-full border border-primary bg-background font-display text-sm text-primary">{rank.day}</span>{index < shown.length - 1 && <span className="hidden h-px flex-1 bg-border lg:block" />}</div><p className="font-display text-lg uppercase">{rank.name}</p><p className="mt-1 text-xs uppercase tracking-widest text-muted">day {rank.day}</p></li>)}</ol></section>; }
