import { SectionHeading } from "@/components/ui/section-heading";
export function Rules() { const items = [["01", "Decide once", "Set your intention. No complicated setup, no dramatic vows."], ["02", "Check in daily", "Tap the button, tell the truth, keep the streak moving."], ["03", "Climb together", "Earn ranks and let your squad bring the right kind of pressure."]]; return <section id="how-it-works" className="mx-auto max-w-6xl scroll-mt-24 px-5 py-20 sm:py-28 lg:px-8 lg:py-36"><SectionHeading eyebrow="The deal" title="The rules are embarrassingly simple" subcopy="No lectures. No shame spiral. Just one honest decision, made every day."/><ol className="mt-14 grid gap-0 border-t border-border sm:mt-20 md:grid-cols-3">{items.map(([number, title, text]) => <li key={number} className="border-b border-border py-7 md:border-b-0 md:border-r md:px-8 md:first:pl-0 md:last:border-r-0"><span className="font-display text-6xl text-primary">{number}</span><h3 className="mt-6 font-display text-2xl uppercase">{title}</h3><p className="mt-3 text-sm leading-relaxed text-muted">{text}</p></li>)}</ol></section>; }
